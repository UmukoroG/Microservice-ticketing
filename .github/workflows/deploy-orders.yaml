#run this deployment workflow if there is a change in the orders directory upon a commit
name: deploy-orders 

on:
  push:
    branches:
      - master
    paths:
      - 'orders/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd orders && docker build -t umukorog2/orders .
      - run: docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      - run: docker tag umukorog2/orders umukorog2/dockerhub
      - run: docker push umukorog2/dockerhub      
      - uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
      - run: doctl kubernetes cluster kubeconfig save a1f7173e-1877-47d5-9d16-80380cc322f5
      - run: kubectl rollout restart deployment orders-depl #restart the deployment
      
    
